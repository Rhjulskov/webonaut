machine:
  pre:
    - curl -o meteor_install_script.sh https://install.meteor.com/
    - npm install -g modulus



## Customize deployment commands
deployment:
  production:
    branch: master
    commands:
        - modulus deploy -p "webonaut"
      ##- git push scalingo master
